## Instruções de Execução
- Instale as dependências executando npm install 
- Crie um arquivo .env e sete as variáveis ATLAS_URI (endereço da instância do MongoDB) e PORT (para especificar a porta que a aplicação irá utilizar).
